overwrite: true
schema: introspection.json
generates:
  src/apollo/types.ts:
    schema: introspection.json
    plugins:
      - "typescript"
    config:
      dedupeOperationSuffix: true
      enumsAsTypes: true
      skipTypename: true
